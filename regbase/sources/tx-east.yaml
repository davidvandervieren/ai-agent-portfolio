# regbase/sources/tx-east.yaml
#
# STATUS: BLOCKED — NO RECORDS WRITTEN THIS RUN.
#
# This file was generated by a research agent tasked with building a
# machine-readable regulatory source registry for TX-East (Eagle Ford,
# Barnett/DFW, East TX/Haynesville, Gulf Coast) counties, municipalities,
# and Gulf-Coast special districts (GLO/CMP, navigation districts, port
# authorities, drainage/flood control districts).
#
# Per the task's standing rule ("never guess" / "NEVER invent or
# reconstruct a URL — transcribe URLs EXACTLY as they appear in
# WebSearch results, or mark confidence: unverified using an agency
# landing page you actually saw"), records require at least one live
# lookup to seed a real, transcribed URL.
#
# At task start, the session's WebSearch tool budget was ALREADY AT
# 0 of 200 remaining (consumed by other activity earlier in this
# session, not by this task) — every WebSearch call returned
# "web search budget ... used" with zero results, confirmed on retry.
# WebFetch, curl, and wget were explicitly disallowed by the task
# instructions (blocked by the network egress proxy). No other
# research tool was available.
#
# Result: zero URLs were actually seen this run, so zero source
# records could be written without violating the no-guessing /
# no-invented-URL rule. Writing 150+ fabricated municode/ecode360/
# county-site/GIS URLs from training memory (which are frequently
# wrong, renamed, or restructured — county sites in particular churn
# often) would be actively harmful in a registry meant to be
# machine-consumed and trusted.
#
# See tx-east.notes.md in this directory for: the full jurisdiction
# checklist still to be researched, the exact WebSearch queries queued
# up and ready to run, and the substantive (non-URL) legal/structural
# analysis the task requested (HB 40 post-preemption DFW ordinance
# landscape, navigation/drainage district crossing-permit landscape,
# GLO/CMP coastal triggers) based on established general knowledge,
# each flagged for citation verification once search access is
# restored.
#
# NEXT STEP: re-run this task (same scope, same output path) once
# WebSearch budget is available (raise
# CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION or run in a fresh session),
# or grant WebFetch access to a proxy-allowed egress path.

sources: []
